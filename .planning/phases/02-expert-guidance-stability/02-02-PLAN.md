---
phase: 02-expert-guidance-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blend_ai/prompts/workflows.py
  - tests/test_tools/test_prompts.py
autonomous: true
requirements:
  - GUIDE-01
  - GUIDE-02
  - GUIDE-03
  - GUIDE-06

must_haves:
  truths:
    - "LLM receives quad topology and edge flow guidance via topology_best_practices prompt"
    - "LLM receives real-world scale references via scale_reference_guide prompt"
    - "LLM receives three-point lighting and HDRI guidance via lighting_principles prompt"
    - "LLM receives multi-step studio lighting setup workflow via studio_lighting_setup prompt"
    - "LLM receives multi-step character basemesh workflow via character_basemesh_workflow prompt"
  artifacts:
    - path: "src/blend_ai/prompts/workflows.py"
      provides: "Expert prompt functions"
      exports: ["topology_best_practices", "scale_reference_guide", "lighting_principles", "studio_lighting_setup", "character_basemesh_workflow"]
    - path: "tests/test_tools/test_prompts.py"
      provides: "Tests for all expert prompts"
      contains: "TestTopologyPrompt"
  key_links:
    - from: "src/blend_ai/prompts/workflows.py"
      to: "blend_ai.server.mcp"
      via: "@mcp.prompt() decorator"
      pattern: "@mcp\\.prompt\\(\\)"
---

<objective>
Add expert MCP prompts for topology, scale reference, lighting principles, and advanced multi-step workflows.

Purpose: The LLM currently has basic best practices and workflow prompts but lacks domain-expert guidance on mesh topology, real-world dimensions, lighting setup, and professional workflow patterns. These prompts transform a generic LLM into a 3D-knowledgeable assistant.

Output: Six new @mcp.prompt() functions in workflows.py with comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-expert-guidance-stability/02-RESEARCH.md

@src/blend_ai/prompts/workflows.py
@src/blend_ai/server.py
@tests/test_tools/conftest.py
</context>

<interfaces>
<!-- The conftest.py mocks mcp.prompt() as an identity decorator, so prompt functions
     can be imported and called directly in tests. -->

From tests/test_tools/conftest.py:
```python
_mcp_mock.tool.return_value = lambda fn: fn  # @mcp.tool() -> identity decorator
# @mcp.prompt() follows the same pattern — returns the function unchanged
```

From src/blend_ai/prompts/workflows.py (existing pattern):
```python
from blend_ai.server import mcp

@mcp.prompt()
def blender_best_practices() -> str:
    """Best practices for using Blender MCP tools effectively."""
    return "..."
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for expert prompts</name>
  <files>tests/test_tools/test_prompts.py</files>
  <read_first>
    - src/blend_ai/prompts/workflows.py (existing prompt pattern — @mcp.prompt() returning str)
    - tests/test_tools/conftest.py (mock server setup — _mcp_mock.tool.return_value = lambda fn: fn)
    - tests/test_tools/test_scene.py (test class/fixture pattern for reference)
  </read_first>
  <behavior>
    - Test: topology_best_practices() returns a non-empty string containing "quad" (case-insensitive)
    - Test: topology_best_practices() contains "edge loop" or "edge flow"
    - Test: topology_best_practices() contains "modifier" (guidance on modifiers vs direct editing)
    - Test: topology_best_practices() contains "n-gon" or "ngon" (warning about n-gons)
    - Test: scale_reference_guide() returns non-empty string containing "meter" or "cm"
    - Test: scale_reference_guide() contains at least 3 real-world object references (e.g., "door", "table", "person")
    - Test: lighting_principles() returns non-empty string containing "three-point" or "3-point"
    - Test: lighting_principles() contains "HDRI"
    - Test: lighting_principles() contains "EEVEE" and "Cycles"
    - Test: studio_lighting_setup() returns non-empty string containing numbered steps (at least "1." and "2.")
    - Test: studio_lighting_setup() contains "key light" or "Key Light"
    - Test: character_basemesh_workflow() returns non-empty string with numbered steps
    - Test: character_basemesh_workflow() contains "mirror" (mirror modifier for symmetry)
    - Test: material_workflow_guide() returns non-empty string containing "Principled BSDF"
    - Test: material_workflow_guide() contains "roughness" and "metallic"
  </behavior>
  <action>
    Create tests/test_tools/test_prompts.py. The conftest.py in tests/test_tools/ already mocks `blend_ai.server` so that `@mcp.prompt()` is an identity decorator. The test file needs to add `@mcp.prompt()` to the mock's available decorators first:

    At the top of the test file, ensure the mock handles prompt():
    ```python
    import sys
    # Ensure mcp.prompt() works as identity decorator (same as mcp.tool())
    _server = sys.modules["blend_ai.server"]
    _server.mcp.prompt.return_value = lambda fn: fn
    ```

    Then import the prompt functions:
    ```python
    from blend_ai.prompts.workflows import (
        topology_best_practices,
        scale_reference_guide,
        lighting_principles,
        studio_lighting_setup,
        character_basemesh_workflow,
        material_workflow_guide,
    )
    ```

    Write test classes: TestTopologyPrompt, TestScalePrompt, TestLightingPrompt, TestStudioLightingWorkflow, TestCharacterBasemeshWorkflow, TestMaterialWorkflowGuide. Each class tests the return string for required content keywords using `assert "keyword" in result.lower()` pattern.

    All tests should FAIL initially (RED phase) because the prompt functions do not exist yet.
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_prompts.py -x -v 2>&1 | head -40</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_tools/test_prompts.py exists
    - grep -q "TestTopologyPrompt" tests/test_tools/test_prompts.py returns 0
    - grep -q "TestScalePrompt" tests/test_tools/test_prompts.py returns 0
    - grep -q "TestLightingPrompt" tests/test_tools/test_prompts.py returns 0
    - grep -q "TestStudioLightingWorkflow" tests/test_tools/test_prompts.py returns 0
    - grep -q "TestCharacterBasemeshWorkflow" tests/test_tools/test_prompts.py returns 0
    - grep -q "TestMaterialWorkflowGuide" tests/test_tools/test_prompts.py returns 0
    - grep -c "def test_" tests/test_tools/test_prompts.py returns at least 14
  </acceptance_criteria>
  <done>Test file created with at least 14 test methods across 6 test classes covering all prompt content requirements</done>
</task>

<task type="auto">
  <name>Task 2: Implement expert prompts in workflows.py</name>
  <files>src/blend_ai/prompts/workflows.py</files>
  <read_first>
    - src/blend_ai/prompts/workflows.py (current file — 5 existing prompts to extend)
    - tests/test_tools/test_prompts.py (test expectations to satisfy)
  </read_first>
  <action>
    Add 6 new @mcp.prompt() functions to src/blend_ai/prompts/workflows.py after the existing functions. Each returns a plain str with expert guidance content.

    1. `topology_best_practices() -> str` (GUIDE-01):
       Docstring: "Expert guide on quad topology, edge flow, and modeling best practices."
       Content must include:
       - Quad topology: prefer quads, avoid triangles/n-gons for subdivision
       - Edge loops: follow natural contours, support deformation for animation
       - Poles: avoid 5+ edge poles in high-curvature areas
       - Modifiers vs direct editing: when to use Mirror, Subsurf, Bevel vs manual loop-cuts
       - N-gon cleanup: use triangulate as last resort, grid-fill preferred
       - Face density: keep even distribution, avoid concentration

    2. `scale_reference_guide() -> str` (GUIDE-02):
       Docstring: "Real-world scale references for common objects in Blender."
       Content must include:
       - Blender units = meters by default
       - At least 8 real-world objects with dimensions in meters and cm:
         door (2.1m), table (0.75m height), chair (0.45m seat), person (1.75m),
         car (4.5m length), coffee cup (0.09m / 9cm), smartphone (0.15m),
         room ceiling (2.4m)
       - Guidance on setting unit system to METRIC
       - Tip: use reference images and measurement tool

    3. `lighting_principles() -> str` (GUIDE-03):
       Docstring: "Lighting principles for professional 3D rendering."
       Content must include:
       - Three-point lighting: key light, fill light, rim/back light with energy ratios
       - HDRI environment lighting: when to use, how to set up via World nodes
       - EEVEE vs Cycles tradeoffs: speed vs accuracy, when to choose each
       - Color temperature: warm vs cool lighting effects
       - Shadow settings: soft shadows for realism

    4. `studio_lighting_setup() -> str` (GUIDE-06):
       Docstring: "Step-by-step guide for professional studio lighting setup."
       Content must include numbered steps:
       1. Create backdrop plane
       2. Add key light (area light, 45-degree angle, ~1000W)
       3. Add fill light (opposite side, ~300W, softer)
       4. Add rim light (behind subject, ~500W)
       5. Set world background to dark/black
       6. Configure render settings (Cycles 256 samples or EEVEE)
       Must contain "key light" or "Key Light" keyword.

    5. `character_basemesh_workflow() -> str` (GUIDE-06):
       Docstring: "Step-by-step workflow for creating a character base mesh."
       Content must include numbered steps:
       1. Start with cube or sphere
       2. Add mirror modifier on X axis for symmetry
       3. Work in edit mode to shape torso proportions
       4. Extrude limbs (arms, legs) from torso
       5. Shape head from top vertices
       6. Add subdivision surface modifier
       7. Refine edge loops for joints
       Must contain "mirror" keyword.

    6. `material_workflow_guide() -> str` (GUIDE-06):
       Docstring: "Expert guide on PBR material setup using Principled BSDF."
       Content must include:
       - Principled BSDF as the standard shader
       - Key parameters: roughness, metallic, base color, normal map
       - Common material recipes: metal (metallic=1.0, roughness=0.2-0.5), plastic (metallic=0.0, roughness=0.3-0.6), glass (transmission=1.0)
       - Texture workflow: image texture nodes for diffuse/roughness/normal
       - Node Wrangler extension recommendation for faster node editing

    Keep the same code style as existing prompts: `@mcp.prompt()` decorator, single `return (...)` with multi-line string concatenation.
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_prompts.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def topology_best_practices" src/blend_ai/prompts/workflows.py returns 0
    - grep -q "def scale_reference_guide" src/blend_ai/prompts/workflows.py returns 0
    - grep -q "def lighting_principles" src/blend_ai/prompts/workflows.py returns 0
    - grep -q "def studio_lighting_setup" src/blend_ai/prompts/workflows.py returns 0
    - grep -q "def character_basemesh_workflow" src/blend_ai/prompts/workflows.py returns 0
    - grep -q "def material_workflow_guide" src/blend_ai/prompts/workflows.py returns 0
    - grep -c "@mcp.prompt()" src/blend_ai/prompts/workflows.py returns at least 11 (5 existing + 6 new)
    - uv run pytest tests/test_tools/test_prompts.py -x passes with 0 failures
  </acceptance_criteria>
  <done>All 6 expert prompt functions implemented, all prompt tests pass, existing prompts unchanged</done>
</task>

</tasks>

<verification>
- uv run pytest tests/test_tools/test_prompts.py -x -v
- uv run pytest tests/ -x (full suite still passes)
</verification>

<success_criteria>
- 6 new expert prompts registered with @mcp.prompt()
- Each prompt contains domain-expert guidance with specific, actionable content
- All 14+ tests pass
- Existing prompts (blender_best_practices, product_shot_setup, etc.) unchanged and still work
</success_criteria>

<output>
After completion, create `.planning/phases/02-expert-guidance-stability/02-02-SUMMARY.md`
</output>
