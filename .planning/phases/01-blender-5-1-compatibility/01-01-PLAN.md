---
phase: 01-blender-5-1-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blend_ai/tools/rendering.py
  - src/blend_ai/tools/scene.py
  - src/blend_ai/tools/sculpting.py
  - addon/handlers/sculpting.py
  - tests/test_tools/test_rendering.py
  - tests/test_tools/test_sculpting.py
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-03, COMPAT-05]

must_haves:
  truths:
    - "BLENDER_EEVEE is accepted as a valid render engine by MCP tools"
    - "BLENDER_EEVEE_NEXT is rejected as invalid by MCP tools"
    - "stroke_method is a settable brush property via MCP tools and handler"
    - "No VSE strip time properties (frame_final_duration, frame_final_start, frame_offset_start) exist anywhere in the codebase"
    - "No scene.node_tree references exist for compositor access in handlers"
  artifacts:
    - path: "src/blend_ai/tools/rendering.py"
      provides: "EEVEE engine constant"
      contains: "BLENDER_EEVEE"
    - path: "src/blend_ai/tools/scene.py"
      provides: "EEVEE engine constant"
      contains: "BLENDER_EEVEE"
    - path: "src/blend_ai/tools/sculpting.py"
      provides: "stroke_method in allowed brush properties"
      contains: "stroke_method"
    - path: "addon/handlers/sculpting.py"
      provides: "stroke_method handler branch"
      contains: "stroke_method"
  key_links:
    - from: "src/blend_ai/tools/rendering.py"
      to: "tests/test_tools/test_rendering.py"
      via: "test validates BLENDER_EEVEE accepted, BLENDER_EEVEE_NEXT rejected"
      pattern: "BLENDER_EEVEE"
    - from: "addon/handlers/sculpting.py"
      to: "src/blend_ai/tools/sculpting.py"
      via: "both allow stroke_method as valid property"
      pattern: "stroke_method"
---

<objective>
Fix EEVEE engine identifier rename (COMPAT-01), add sculpt brush stroke_method support (COMPAT-03), and verify compositor/VSE audits (COMPAT-02, COMPAT-05).

Purpose: Make the core rendering and sculpting tools 5.1-compatible. EEVEE rename affects two MCP tool files and tests. Sculpt stroke_method adds a new settable property. Compositor and VSE are confirmed no-ops by audit but must be verified in tests.
Output: Updated rendering constants, sculpting handler/tool, and passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-blender-5-1-compatibility/01-RESEARCH.md
@src/blend_ai/tools/rendering.py
@src/blend_ai/tools/scene.py
@src/blend_ai/tools/sculpting.py
@addon/handlers/sculpting.py
@tests/test_tools/test_rendering.py
@tests/test_tools/test_sculpting.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update tests for EEVEE rename and stroke_method, add audit tests</name>
  <files>tests/test_tools/test_rendering.py, tests/test_tools/test_sculpting.py</files>
  <read_first>
    - tests/test_tools/test_rendering.py (current EEVEE test assertions to invert)
    - tests/test_tools/test_sculpting.py (current brush property tests to extend)
    - src/blend_ai/tools/rendering.py (current ALLOWED_RENDER_ENGINES constant)
    - src/blend_ai/tools/sculpting.py (current ALLOWED_BRUSH_PROPERTIES set)
  </read_first>
  <behavior>
    - test_set_eevee: calling set_render_engine("BLENDER_EEVEE") succeeds and sends command
    - test_invalid_engine: calling set_render_engine("BLENDER_EEVEE_NEXT") raises ValidationError
    - test_set_brush_property_stroke_method: calling set_brush_property with property="stroke_method" and value="DOTS" succeeds and sends command
    - test_no_vse_deprecated_properties: grep-style assertion that no file in addon/ or src/ contains "frame_final_duration" or "frame_final_start" or "frame_offset_start"
    - test_no_scene_node_tree_compositor: grep-style assertion that addon/handlers/scene.py does not contain "scene.node_tree"
  </behavior>
  <action>
    In tests/test_tools/test_rendering.py:
    1. Change TestSetRenderEngine.test_set_eevee to call set_render_engine("BLENDER_EEVEE") instead of "BLENDER_EEVEE_NEXT". Assert mock_conn.send_command called with {"engine": "BLENDER_EEVEE"}.
    2. Change TestSetRenderEngine.test_invalid_engine to call set_render_engine("BLENDER_EEVEE_NEXT") and assert it raises ValidationError. Update the comment from "old name, not allowed" to "removed in 5.0, not allowed".
    3. Add a test test_invalid_engine_eevee_next that is an alias/duplicate confirming BLENDER_EEVEE_NEXT raises (optional, the renamed test_invalid_engine covers it).

    In tests/test_tools/test_sculpting.py:
    4. Add "stroke_method" to ALLOWED_BRUSH_PROPERTIES in the test module if it imports/references the constant.
    5. Add a test test_set_brush_property_stroke_method that calls set_brush_property(object_name="Cube", property="stroke_method", value="DOTS") and asserts send_command called with {"object_name": "Cube", "property": "stroke_method", "value": "DOTS"}.

    For COMPAT-02 audit (no code change needed):
    6. Add a standalone test (in test_rendering.py or a new test_compat_audit.py) that uses subprocess or pathlib to verify no file in addon/handlers/scene.py contains the string "scene.node_tree" (compositor access pattern). This is a codebase audit test.

    For COMPAT-05 audit (no code change needed):
    7. Add a test that scans addon/ and src/ directories for the strings "frame_final_duration", "frame_final_start", "frame_offset_start" and asserts zero matches. This ensures COMPAT-05 stays satisfied.

    These tests MUST fail before Task 2 implements the production code changes (for EEVEE and stroke_method tests). The audit tests should pass immediately since the codebase already lacks those patterns.
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_rendering.py::TestSetRenderEngine::test_invalid_engine -x 2>&1 | grep -q "PASSED" && echo "EEVEE_NEXT rejection test passes (expected to fail until Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tools/test_rendering.py contains `set_render_engine("BLENDER_EEVEE")` in test_set_eevee
    - tests/test_tools/test_rendering.py contains `set_render_engine("BLENDER_EEVEE_NEXT")` inside a `pytest.raises(ValidationError)` block
    - tests/test_tools/test_sculpting.py contains a test function named `test_set_brush_property_stroke_method`
    - tests/test_tools/test_sculpting.py contains the string `"stroke_method"` in a send_command assertion
    - A test exists that scans for "frame_final_duration" in addon/ and src/ and asserts zero matches
    - A test exists that verifies addon/handlers/scene.py does not contain "scene.node_tree"
  </acceptance_criteria>
  <done>All new/updated test functions exist. EEVEE and stroke_method tests fail (RED) because production code not yet updated. Audit tests pass (codebase already clean).</done>
</task>

<task type="auto">
  <name>Task 2: Fix EEVEE constant and add stroke_method to sculpting</name>
  <files>src/blend_ai/tools/rendering.py, src/blend_ai/tools/scene.py, src/blend_ai/tools/sculpting.py, addon/handlers/sculpting.py</files>
  <read_first>
    - src/blend_ai/tools/rendering.py (ALLOWED_RENDER_ENGINES on line 16)
    - src/blend_ai/tools/scene.py (ALLOWED_RENDER_ENGINES on line 25)
    - src/blend_ai/tools/sculpting.py (ALLOWED_BRUSH_PROPERTIES on line 18)
    - addon/handlers/sculpting.py (handle_set_brush_property function starting line 89)
  </read_first>
  <action>
    COMPAT-01 — EEVEE identifier rename (per research Pattern 1):

    1. In src/blend_ai/tools/rendering.py line 16, change:
       `ALLOWED_RENDER_ENGINES = {"BLENDER_EEVEE_NEXT", "CYCLES", "BLENDER_WORKBENCH"}`
       to:
       `ALLOWED_RENDER_ENGINES = {"BLENDER_EEVEE", "CYCLES", "BLENDER_WORKBENCH"}`

    2. In src/blend_ai/tools/rendering.py line 30, change docstring:
       `engine: Render engine to use. One of: BLENDER_EEVEE_NEXT, CYCLES, BLENDER_WORKBENCH.`
       to:
       `engine: Render engine to use. One of: BLENDER_EEVEE, CYCLES, BLENDER_WORKBENCH.`

    3. In src/blend_ai/tools/scene.py line 25, change:
       `ALLOWED_RENDER_ENGINES = {"BLENDER_EEVEE_NEXT", "BLENDER_WORKBENCH", "CYCLES", "BLENDER_EEVEE"}`
       to:
       `ALLOWED_RENDER_ENGINES = {"BLENDER_EEVEE", "BLENDER_WORKBENCH", "CYCLES"}`
       Note: Remove BLENDER_EEVEE_NEXT entirely. The old scene.py had BOTH — only keep BLENDER_EEVEE.

    COMPAT-03 — Sculpt brush stroke_method (per research Pattern 4):

    4. In src/blend_ai/tools/sculpting.py line 18, change:
       `ALLOWED_BRUSH_PROPERTIES = {"size", "strength", "auto_smooth_factor", "use_frontface"}`
       to:
       `ALLOWED_BRUSH_PROPERTIES = {"size", "strength", "auto_smooth_factor", "use_frontface", "stroke_method"}`

    5. In addon/handlers/sculpting.py, in handle_set_brush_property (after the `use_frontface` elif block at line 106, before the `else` at line 107), add:
       ```python
       elif prop == "stroke_method":
           ALLOWED_STROKE_METHODS = {
               "DOTS", "DRAG_DOT", "SPACE", "AIRBRUSH",
               "ANCHORED", "LINE", "CURVE",
           }
           if value not in ALLOWED_STROKE_METHODS:
               raise ValueError(
                   f"stroke_method '{value}' not in "
                   f"{sorted(ALLOWED_STROKE_METHODS)}"
               )
           brush.stroke_method = value
       ```

    No changes needed for COMPAT-02 or COMPAT-05 — audit tests from Task 1 confirm the codebase is already clean.
  </action>
  <verify>
    <automated>cd /Users/michael/code/blend-ai && uv run pytest tests/test_tools/test_rendering.py tests/test_tools/test_sculpting.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/blend_ai/tools/rendering.py contains `"BLENDER_EEVEE"` in ALLOWED_RENDER_ENGINES (not BLENDER_EEVEE_NEXT)
    - src/blend_ai/tools/rendering.py does NOT contain the string `BLENDER_EEVEE_NEXT`
    - src/blend_ai/tools/scene.py contains `"BLENDER_EEVEE"` in ALLOWED_RENDER_ENGINES (not BLENDER_EEVEE_NEXT)
    - src/blend_ai/tools/scene.py does NOT contain the string `BLENDER_EEVEE_NEXT`
    - src/blend_ai/tools/sculpting.py contains `"stroke_method"` in ALLOWED_BRUSH_PROPERTIES
    - addon/handlers/sculpting.py contains `elif prop == "stroke_method":`
    - addon/handlers/sculpting.py contains `ALLOWED_STROKE_METHODS`
    - addon/handlers/sculpting.py contains `brush.stroke_method = value`
    - `uv run pytest tests/test_tools/test_rendering.py tests/test_tools/test_sculpting.py -x` exits 0
  </acceptance_criteria>
  <done>BLENDER_EEVEE is the only EEVEE identifier in the codebase. stroke_method is settable via MCP tool and handler. All rendering and sculpting tests pass (GREEN).</done>
</task>

</tasks>

<verification>
1. `grep -r "BLENDER_EEVEE_NEXT" src/ addon/ tests/` returns only test lines that assert it is INVALID
2. `grep -r "stroke_method" src/ addon/` shows it in both sculpting.py files
3. `grep -r "frame_final_duration\|frame_final_start\|frame_offset_start" src/ addon/` returns zero matches
4. `grep "scene\.node_tree" addon/handlers/scene.py` returns zero matches
5. `uv run pytest tests/test_tools/test_rendering.py tests/test_tools/test_sculpting.py -v` all pass
</verification>

<success_criteria>
- BLENDER_EEVEE accepted, BLENDER_EEVEE_NEXT rejected across all MCP tools
- stroke_method is a valid settable brush property
- Codebase audit confirms no compositor scene.node_tree usage and no VSE deprecated properties
- All rendering and sculpting tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-blender-5-1-compatibility/01-01-SUMMARY.md`
</output>
